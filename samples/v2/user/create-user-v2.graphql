# Create a new user

mutation CreateUserV2($input: CreateUserV2Input!) {
  createUserV2(input: $input) {
    bio
    createdPrograms
    email
    firstName
    lastName
    links
    loginType
    organizationName
  }
}
