# Get a single user by ID

query UserV2($id: ID!) {
  userV2(id: $id) {
    bio
    createdPrograms
    email
    firstName
    lastName
    links
    loginType
    organizationName
    profileImage
    role
    skills
    walletAddress
  }
}
