# Get a single application by ID

query ApplicationV2($id: ID!) {
  applicationV2(id: $id) {
    applicant
    applicantId
    content
    picked
    program
    programId
    rejectedReason
    status
  }
}
