# Create a new thread

mutation CreateThreadV2($input: CreateThreadInput!) {
  createThread(input: $input) {
    authorNickname
    authorProfileImage
    content
    dislikeCount
    images
    isDisliked
    isLiked
    likeCount
  }
}
